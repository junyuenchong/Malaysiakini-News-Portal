# News Portal — environment template
#
# Local:      cp .env.development .env && php artisan key:generate
# Production: cp .env.production .env && php artisan key:generate
#
# Do not commit your active .env file.
