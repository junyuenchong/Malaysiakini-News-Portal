# Production
# Setup: cp .env.production .env && php artisan key:generate
# Set APP_KEY and APP_URL before deploy.
# Database: configure via RDS / managed MySQL in your deploy platform (not in this file).

# Application
APP_NAME=NewsPortal
APP_ENV=production
APP_KEY=
APP_DEBUG=false
APP_URL=https://your-domain.com
